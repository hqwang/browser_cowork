打开网站【subo拆解DAG图】上市信息—十大股东/主要股东功能优化 XQ-20250628602，提取内容：
xqID：从标题里提取XQ-{xqID}
xqType：子任务拆解下面的角色，包括客户端/前端/服务端任一为 功能型；否则为 数据型。
suboList：从子任务拆解下方的表格进行拆解，为数组类型[]，数组保存的是任务单元列表。[]提取规则如下：
[].pointType：`流程节点`列
[].pointPerson：`节点负责人`列
[].spendHour：`估时`列中的时间(数字+小数，不带单位)
[].pointName：`估时`列中的任务单元
提取完成后，按照json格式打印出来。
